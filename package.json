{
  "name": "blockbreaker",
  "version": "1.0.0",
  "description": "Jeu Block Breaker avec Firebase",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase": "^11.7.1"
  }
}
